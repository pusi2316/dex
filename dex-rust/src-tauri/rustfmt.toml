# Maximum line length before wrapping (default is 100)
max_width = 120

# Force use of Unix-style line endings (\n)
newline_style = "Unix"

# Convert block comments to line comments automatically
comment_width = 80
wrap_comments = true

# Reorder import (use) statements alphabetically
reorder_imports = true
